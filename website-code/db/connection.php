<?php

/* 
 * The database connection string.
 * Should switch this to PDO.
 */

$host = 'localhost';
$username='deleted';
$password='deleted';
$dbname='deleted';
 
$db = mysqli_connect($host, $username, $password, $dbname);

$adminPanel = "password";